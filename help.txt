
/* === ОСНОВНЫЕ НАСТРОЙКИ СТРАНИЦЫ === */
body {
  position: relative;           /* для позиционирования псевдоэлемента */
  min-height: 100vh;            /* минимальная высота на весь экран */
  margin: 0;                    /* убираем отступы по умолчанию */
  display: flex;                /* включаем flex-контейнер */
  flex-direction: column;       /* элементы располагаются вертикально */
  justify-content: center;      /* центрируем по вертикали */
  align-items: center;          /* центрируем по горизонтали */
}

/* Псевдоэлемент для фонового изображения (позволяет регулировать прозрачность) */
body::before {
  content: "";                  /* обязательное свойство для псевдоэлемента */
  position: fixed;              /* фиксируем относительно окна браузера */
  top: 0; left: 0;              /* прижимаем к левому верхнему углу */
  width: 100%; height: 100%;    /* растягиваем на весь экран */
  background-image: url('image.png'); /* твоя картинка */
  background-size: cover;        /* масштабируем, чтобы покрыть всю область */
  background-position: center;   /* центрируем изображение */
  background-repeat: no-repeat;  /* не повторяем */
  opacity: 0.3;                  /* прозрачность 30% (можно менять) */
  z-index: -1;                   /* помещаем позади всего контента */
}

/* === СТИЛИ ДЛЯ ВСЕХ КНОПОК (ОСНОВНОЙ КЛАСС) === */
.my-btn {
  margin-right: 5px;            /* отступ справа */
  margin-top: 5px;              /* отступ сверху */
  width: 50px;                  /* ширина кнопки */
  height: 50px;                 /* высота кнопки */
  border-radius: 0%;            /* квадратная форма (0% — острые углы) */
  border: none;                 /* убираем обводку */
  background: #e193ae;          /* нежно-розовый фон */
  color: white;                 /* белый текст */
  font-size: 1.5rem;            /* размер шрифта */
  cursor: pointer;              /* курсор-указатель */
  user-select: none;            /* запрет выделения текста */
  font-family: 'Georgia', monospace; /* шрифт */
  font-weight: normal;          /* обычная толщина */
}

/* Состояние при наведении курсора на кнопку */
.my-btn:hover {
  background: #e193ae;          /* пока оставлен тот же цвет, можно поменять */
}

/* Состояние при нажатии на кнопку */
.my-btn:active {
  filter: brightness(130%);     /* небольшое осветление */
}

/* Кнопки основных операций (/, x, -, +, =) */
.my-btn.primary {
  background: #ba5577;          /* более тёмный розовый */
}

/* Кнопки второстепенных операций (C, +/-, %) */
.my-btn.secondary {
  background: #8e365c;          /* ещё более тёмный, ближе к бордовому */
}

/* Кнопка "=" (отдельный класс для ширины) */
.my-btn.execute {
  width: 110px;                 /* шире остальных */
  border-radius: 0;             /* квадратная, без скругления */
}

/* === ПОЛЕ ВЫВОДА РЕЗУЛЬТАТА === */
.result {
  width: 220px;                 /* ширина поля */
  height: 80px;                 /* высота (увеличена по заданию 11) */
  border-radius: 10px;          /* скруглённые углы */
  margin-bottom: 15px;          /* отступ снизу */
  padding-right: 10px;          /* внутренний отступ справа */
  background: #ba5577;          /* цвет фона (под цвет primary) */
  text-align: right;            /* текст прижат к правому краю */
  color: #ffffff;               /* белый текст */
  font-weight: bold;            /* жирный */
  font-size: 1.5rem;            /* размер шрифта */
  font-family: 'Georgia', monospace; /* шрифт как у кнопок */
}

/* === ПОДПИСЬ "ЛР ВЫПОЛНЕНА ФИО" === */
.signature {
  color: rgb(0, 0, 0);          /* чёрный цвет (можно изменить) */
  font-size: 1.8rem;            /* крупный шрифт */
  margin-top: 35px;             /* отступ сверху (чтобы отделить от калькулятора) */
  font-family: 'Georgia', monospace;
  font-weight: bold;            /* жирный */
}

/* === КНОПКА-ССЫЛКА НА GITHUB === */
.github {
  margin-top: 25px;             /* отступ сверху */
  width: 200px;                 /* ширина */
  height: 50px;                 /* высота */
  border-radius: 0%;            /* квадратная */
  background: #e193ae;          /* цвет как у обычных кнопок */
  color: white;                 /* белый текст */
  font-size: 1.5rem;            /* размер шрифта */
  cursor: pointer;              /* курсор-указатель */
  user-select: none;            /* запрет выделения */
  font-family: 'Georgia', monospace;
  font-weight: bold;            /* жирный */
  text-align: center;           /* текст по центру по горизонтали */
  text-decoration: none;        /* убираем подчёркивание у ссылки */
  line-height: 50px;            /* вертикальное выравнивание текста (по центру) */
}
